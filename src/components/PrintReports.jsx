import { Button } from 'react-bootstrap';

function PrintReports() {
  return (
      <div>
          <Button variant="primary" className="w-100 mb-2">Print Reports</Button>
      </div>
  )
}

export default PrintReports
